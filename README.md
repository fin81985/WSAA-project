This project is an Expense Tracker web application built using Flask.

The application allows users to:
- Create expenses
- View expenses
- Update expenses
- Delete expenses

The system uses a RESTful API connected to a SQLite database.
The frontend communicates with the API using JavaScript (AJAX).
